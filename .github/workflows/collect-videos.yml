name: Collect YouTube Videos

on:
  schedule:
    # 매시간 정각에 실행
    - cron: '0 * * * *'
  workflow_dispatch:
    # 수동 실행 가능

jobs:
  collect:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install Puppeteer dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libgbm-dev

      - name: Run collection script
        run: npm run collect
        env:
          GITHUB_ACTIONS: true
          BATCH_SIZE: '20'
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}

      - name: Check if full cycle complete
        id: check_cycle
        run: |
          if [ -f "data/collect-checkpoint.json" ]; then
            echo "checkpoint_exists=true" >> $GITHUB_OUTPUT
            echo "Checkpoint exists - batch in progress"
          else
            echo "checkpoint_exists=false" >> $GITHUB_OUTPUT
            echo "No checkpoint - full cycle complete!"
          fi

      - name: Send Telegram viral notification
        if: steps.check_cycle.outputs.checkpoint_exists == 'false'
        run: npm run telegram-notify
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          VIRAL_THRESHOLD: 500
          TOP_VIRAL_COUNT: 10
        continue-on-error: true

      - name: Commit and push changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: 'chore: auto-collect videos [skip ci]'
          file_pattern: 'data/*.json'
          commit_user_name: 'GitHub Actions Bot'
          commit_user_email: 'actions@github.com'
