# PocketMonTube 작업 로그

## 2026-01-07: InnerTube API 수집 안정화

### 문제 상황

1. **체크포인트 멈춤**: 그룹 0, 채널 30에서 9시간 이상 멈춤
2. **에러 핸들링 문제**: `throw error`로 전체 프로세스 종료되어 복구 불가
3. **텔레그램 알림 실패**: 사이클 미완료로 과거 데이터로 알림 발송
4. **데이터 품질**: 많은 영상의 viewCount=0, duration="0:00"

### 수정 내용

#### 1. `scripts/collect-videos.ts` 수정

##### 1-1. 에러 로그 시스템 추가 (103줄 후)
- `CollectError` 인터페이스: 에러 정보 구조화
- `CollectStats` 인터페이스: 수집 통계 추적
- `logCollectError()`: 에러를 `data/collect-errors.json`에 저장 (최근 500개 유지)
- `saveStats()`: 통계를 `data/collect-stats.json`에 저장
- `logProgress()`: 타임스탬프 포함 로그 출력

##### 1-2. `fetchVideoDetails` 재시도 로직 (181줄)
- **기존**: 1회 시도 후 실패 시 null 반환
- **수정**: 최대 2회 재시도 (총 3회 시도)
  - 1차 실패: 30초 대기 후 재시도
  - 2차 실패: 60초 대기 후 재시도
  - 3차 실패: 에러 로그 저장, null 반환 (다음 사이클에서 재시도)
- **429 에러 감지**: 60초 × 시도횟수 대기, 클라이언트 재생성
- **viewCount=0 재시도**: API 문제 가능성 있으므로 재시도

##### 1-3. 채널 에러 핸들링 (470줄)
- **기존**: `throw error`로 전체 프로세스 종료
- **수정**: `continue`로 다음 채널 계속 처리
  - 에러 로그 저장
  - 429 에러 시 60초 대기, 그 외 30초 대기
  - 429 에러 시 클라이언트 재생성

##### 1-4. 딜레이 증가
| 항목 | 기존 | 수정 |
|------|------|------|
| 영상당 딜레이 | 3초 | 5초 |
| 채널간 딜레이 | 5초 | 10초 |
| 기존 영상 업데이트 | 3초 | 5초 |

##### 1-5. 진행 로그 강화
- 10개 채널마다 상세 진행 상황 출력 (기존 20개)
- 경과 시간, 에러 수, 재시도 수 포함
- 시작/종료 시 구분선과 요약 정보

#### 2. 파일 변경 사항

| 파일 | 작업 | 설명 |
|------|------|------|
| `scripts/collect-videos.ts` | 수정 | 에러 핸들링, 재시도, 로그 강화 |
| `data/collect-checkpoint.json` | 삭제 | 처음부터 수집 시작 |
| `data/.gitignore` | 생성 | 에러 로그 파일 커밋 제외 |
| `WORK_LOG.md` | 생성 | 작업 기록 문서화 |

### 예상 동작 흐름

```
[GitHub Actions 매시간 실행]
    |
[체크포인트 없음] -> 처음부터 시작
    |
[20개 채널 처리] (BATCH_SIZE=20)
    |-- RSS로 영상 목록 수집
    |-- 각 영상 상세정보 수집 (InnerTube)
    |   |-- 성공 -> 저장
    |   |-- 실패 -> 30초 대기 -> 재시도 1
    |   |-- 실패 -> 60초 대기 -> 재시도 2
    |   |-- 실패 -> 에러 로그 저장, viewCount=0으로 저장
    |-- 채널 완료 -> 체크포인트 저장
    |-- 다음 채널...
    |
[20개 완료] -> 체크포인트 저장, 커밋
    |
[다음 실행] -> 체크포인트에서 이어서...
    |
[전체 132개 완료] -> 체크포인트 삭제
    |
[텔레그램 알림 발송]
```

### 다음 작업자를 위한 참고

#### 프로젝트 구조
- 전체 채널 수: 132개 (6개 그룹)
- 예상 사이클 완료: 7회 실행 (20개 x 7 = 140 > 132)
- GitHub Actions: 매시간 정각 실행 (`0 * * * *`)

#### 주요 파일
- `scripts/collect-videos.ts` - 메인 수집 스크립트
- `scripts/send-telegram-notification.ts` - 텔레그램 알림
- `.github/workflows/collect-videos.yml` - GitHub Actions 워크플로우
- `data/channels.json` - 채널 목록 (132개)
- `data/videos.json` - 영상 데이터

#### 에러 확인 방법
- 에러 로그: `data/collect-errors.json`
- 통계: `data/collect-stats.json`
- GitHub Actions 로그: Actions 탭에서 확인

#### 텔레그램 알림 조건
- `data/collect-checkpoint.json`이 없을 때만 발송
- 즉, 전체 사이클이 완료되어야 알림 발송

### 관련 환경 변수
- `BATCH_SIZE`: 한 번에 처리할 채널 수 (기본값: 20)
- `TELEGRAM_BOT_TOKEN`: 텔레그램 봇 토큰
- `TELEGRAM_CHAT_ID`: 텔레그램 채팅 ID
- `VIRAL_THRESHOLD`: 바이럴 기준 (기본값: 500/h)

### 수정 전/후 비교

| 항목 | 수정 전 | 수정 후 |
|------|---------|---------|
| 에러 시 동작 | 전체 프로세스 종료 | 다음 채널로 계속 |
| 재시도 | 없음 | 최대 2회 |
| 429 대응 | 없음 | 60초 대기 + 클라이언트 재생성 |
| 에러 로그 | 콘솔만 | 파일 저장 |
| 진행 로그 | 20개마다 | 10개마다 + 상세 정보 |
| 사이클 완료 | 에러 시 불가 | 에러 있어도 완료 가능 |

---

## 변경 이력

| 날짜 | 작업자 | 내용 |
|------|--------|------|
| 2026-01-07 | Claude | InnerTube API 수집 안정화 작업 |
